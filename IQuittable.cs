namespace EmployeeApp
{
    // Define an interface named IQuittable which requires implementing classes to provide a Quit method.
    public interface IQuittable
    {
        // The Quit method has no return value (void) and no parameters.
        void Quit();
    }
}
